insurance_case: # this needs to match the profile in your dbt_project.yml file
    target: dev
    outputs:
        dev:
            type: postgres
            host: 127.0.0.1
            user: user
            password: password
            port: 5432
            dbname: insurance_case_db
            schema: datawarehouse
            threads: 1
            keepalives_idle: 0 # default 0, indicating the system default. See below
            connect_timeout: 20 # default 10 seconds
            retries: 1
